# Normalize line endings: store text as LF in the repo, check out native on Windows.
# Stops whole-file "phantom" diffs caused by CRLF/LF differences.
* text=auto

# Treat these as binary so git never tries to alter them.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.pdf binary
